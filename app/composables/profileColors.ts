export function profileColors() {
    const allColors: Record<string, string> = {
        redToIndigo: 'bg-gradient-to-r from-red-400 to-indigo-600',
        blueToPink: 'bg-gradient-to-b from-blue-200 to-pink-600',
    };
    return {
        allColors
    }
}